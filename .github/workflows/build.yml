name: Build .NET SDK

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'dotnet VMR branch name'
        required: true
        type: string

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
    - name: Get Docker image
      run: |
        docker image pull mcr.microsoft.com/dotnet-buildtools/prereqs:azurelinux-3.0-cross-riscv64-net9.0
        
    - name: Maximize build space
      uses: AdityaGarg8/remove-unwanted-software@v4.1
      with:
        remove-dotnet: 'true'
        remove-android: 'true'
        remove-haskell: 'true'
        remove-codeql: 'true'
        remove-cached-tools: 'true'        

    - name: Clone repository
      run: |
        git clone --depth 1 -b ${{ inputs.branch }} https://github.com/dotnet/dotnet

    - name: Apply patches
      run: |
        wget https://gist.githubusercontent.com/filipnavara/9a94666778b095b6518b946ce6bce756/raw/0d5546ccd6da3cf6fad174d21d4e0ef1101f10b0/TwoStageRuntimeBuild.diff
        patch -d dotnet -p4 -i ${{ github.workspace }}/TwoStageRuntimeBuild.diff

    - name: Build
      run: |
        docker run --platform linux/amd64 --rm -v${{ github.workspace }}/dotnet:/dotnet -e ROOTFS_DIR=/crossrootfs/riscv64 -e DISABLE_CROSSGEN=1 \
          mcr.microsoft.com/dotnet-buildtools/prereqs:azurelinux-3.0-cross-riscv64-net9.0 \
          sh -c 'cd /dotnet && ./prep-source-build.sh && ./build.sh --clean-while-building -sb /p:TargetOS=linux /p:TargetArchitecture=riscv64 /p:Crossbuild=true /p:TwoStageRuntimeBuild=true'

    - name: Upload .NET
      uses: actions/upload-artifact@v4
      with:
        name: dotnet-sdk-linux-riscv64
        path: "${{ github.workspace }}/dotnet/artifacts/assets/Release/dotnet-sdk-*"
